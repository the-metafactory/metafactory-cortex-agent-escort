{
  "name": "metafactory-cortex-agent-escort",
  "version": "0.5.0",
  "description": "Escort — cortex onboarding greeter with a closed effect universe. Deterministic by default, stack-managed hybrid voice by opt-in (compose, cortex#2257). Exec brain, surfaces to a human, never grants.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "repository": "github:the-metafactory/metafactory-cortex-agent-escort",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.5.0"
  }
}
